verificar:
    Tipos -> Expresiones (el tipo de los operandos encaja con el operador)
          -> Asignaciones (el tipo de la variable es igual que el tipo de la expresion del lado derecho)
          -> return (tipo de retorno valid0)
          
    Variables -> que esten declaradas (ver que esten en la tabla de simbolos)
              -> que no esten declaradas dos veces en el mismo ambiente (ver que no este ya en el mismo nivel de la tabla de simbolos)

La tabla de simbolos POR AHORA es solo una lista donde los nodos tienen nombre de la variable y tipo. A futuro se le agregaria direccion de memoria para poder pasar a assembly.

Para hacer el arbol AST:
en el parser con bison se le puede poner codigo a cada regla. Entonces:
    si el lado derecho es un terminal (E->nro) a la regla le ponemos el codigo {E = nodo(nro)} (en bison es {$$ = nodo($1)}). Es decir, creamos un nodo para el hijo y se lo asignamos al padre
    si la regla es por ej E-> E1 + E2 a la regla en el parser le ponemos el codigo {$$ = nodo(PLUS, $1, $3)}. Es decir Al padre le asignamos un nodo con el padre + y los hijos E1 y E2 (el $2 seria el signo '+')  !!! NO ESTOY NADA SEGURO DE ESTO (buscar o ver ejemplos)!!!

    hay que hacer la estrucura arbol con nodos y sus metodos (solo 3 metodos dijo pancho, incluyendo el de print) para poder hacer el AST. En flex ademas del token hay que retornar el tipo y valor de la variable. Creo que me falto un paso sobre tipos.

	Hacer el arbol (el ADT) por separado y probarlo antes de integrarlo a bison
